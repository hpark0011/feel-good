---
id: FG_066
title: "Chat code review cleanup: dead code, CSS artifacts, redundant wrappers"
date: 2026-03-10
type: refactor
status: to-do
priority: p3
description: "Consolidation of minor cleanup findings from code review: MOCK_FAILED_MESSAGE is exported but never used, commented-out CSS lines are experimentation artifacts, handleConversationCreated is a pointless wrapper around setConversationId, cn() is used with a single string arg (no-op), and next-env.d.ts changes are auto-generated noise."
dependencies: []
parent_plan_id:
acceptance_criteria:
  - "grep -rn 'MOCK_FAILED_MESSAGE' apps/mirror/ returns zero matches (dead export removed)"
  - "grep -n 'top:' apps/mirror/styles/globals.css returns zero commented-out matches in the message-send keyframe"
  - "grep -n 'handleConversationCreated' apps/mirror/features/chat/components/chat-thread.tsx returns zero matches"
  - "pnpm build --filter=@feel-good/mirror completes without errors"
owner_agent: "Code cleanup specialist"
---

# Chat code review cleanup: dead code, CSS artifacts, redundant wrappers

## Context

Multiple minor findings from the `feat-rag` code review, consolidated into one cleanup ticket.

## Goal

Remove dead code, experimentation artifacts, and unnecessary wrappers from the chat feature.

## Scope

1. **Dead export** — `MOCK_FAILED_MESSAGE` in `apps/mirror/features/chat/lib/mock-chat-data.ts:59-66` is exported but never imported. Delete it.
2. **CSS artifacts** — Commented-out `/* top: ... */` lines in `apps/mirror/styles/globals.css:42,47,52`. Remove them.
3. **Pointless wrapper** — `handleConversationCreated` in `apps/mirror/features/chat/components/chat-thread.tsx:234-239` just calls `setConversationId`. Pass `setConversationId` directly to `useChat`.
4. **Redundant `cn()`** — `cn("single string")` at `chat-thread.tsx:193` does nothing. Use the string directly.
5. **Auto-generated noise** — `next-env.d.ts` changes in greyboard, mirror, ui-factory are auto-generated by Next.js. Either discard or commit separately.

## Out of Scope

- Structural refactoring of ChatActiveThreadLayout
- Simplifying the mock streaming mechanism

## Approach

Direct deletion and simplification. All changes are mechanical with no behavior change.

- **Effort:** Small
- **Risk:** Low

## Implementation Steps

1. Delete `MOCK_FAILED_MESSAGE` export from `mock-chat-data.ts`
2. Remove commented-out `/* top: ... */` lines from `globals.css`
3. Replace `handleConversationCreated` with direct `setConversationId` pass-through in `chat-thread.tsx`
4. Replace `cn("...")` with the string directly at line 193
5. Discard `next-env.d.ts` changes with `git checkout -- apps/*/next-env.d.ts`
6. Run `pnpm build --filter=@feel-good/mirror` to verify

## Constraints

- No behavior changes
- Each sub-item is independently safe to apply

## Resources

- TypeScript reviewer and code simplicity reviewer findings
